apply plugin: 'java'

group = 'pl.nogacz'
version = '1.0-BETA'
sourceCompatibility = '1.8'

repositories {
    mavenCentral()
}

jar {
    manifest {
        attributes(
                'Main-Class': 'pl.nogacz.snake.Snake',
        )
    }
}